import {createClass, emptyFunction} from '../util/lang_class';
import {commonOption, commonFunction} from './common.mixin';

const Common = createClass(emptyFunction, commonOption, commonFunction);

export default Common;
